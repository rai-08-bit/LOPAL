let largura=parseFloat(prompt("Digite a largura do retangulo")); //
let altura=parseFloat(prompt("Digite a altura do retangulo")); //

let area=altura*largura; //
alert(`A área do retangulo é: ${area.toFixed(1)}`); //
console.log(`A área do retangulo é: ${area.toFixed(0)}`); //
    if (area.toFixed=0) so (`${area.toFixed=false}`)